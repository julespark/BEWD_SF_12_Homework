<h1>Add new item</h1>
<%= form_for @loggedin, :url => loggedin_path(@loggedin) do |f| %>
	<%= f.label :name %>: <%= f.text_field :name %>
	<%= f.hidden_field :done, :value => false %>
	<%= f.submit "Add to list" %>
<%end%>