<h1> Welcome to the contacts app </h1>

<div>
<% @contacts.each do |contact| %>
	<div>
		<%= contact.name %>
		<%= contact.phone %>

		<%= form_tag send_text_url(id: contact.id) do |f| %>
			<%= f.text_field :body %>
			<%= submit_tag "Send message" %>
		<%end%>

	</div>
<%end%>
</div>